<div class="container">
  <h2>Add Review for <%= @restaurants.name %></h2>

  <%= simple_form_for([@restaurants, @reviews]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, :collection => Review::RATING %>
  <%= f.submit %>
  <% end %>
  
</div>
